# 预设组件映射（Bash associative array）
declare -A MAIN_PRESET_COMPONENTS=(
    [osx]="conda,prerequisites,stow,zsh_plugin,shell_scripts,extras,brew_bundle"
    [linux-gui]="conda,prerequisites,stow,zsh_plugin,shell_scripts,extras,brew_bundle"
    [linux-tty]=""
    [linux-container]="prerequisites,stow,zsh_plugin,shell_scripts,brew_bundle"
)

declare -A BREW_BUNDLE_PRESET_COMPONENTS=(
    [osx]="Brewfile.osx"
    [linux-gui]="Brewfile.linux-gui"
    [linux-tty]=""
    [linux-container]="Brewfile.linux-container"
)

# 支持的预设列表（可选，也可在主脚本中保留）
SUPPORTED_PRESETS=("osx" "linux-gui" "linux-tty" "linux-container")
